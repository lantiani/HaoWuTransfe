<!--pages/subPages/order/order.wxml-->
<view>
    <van-tabs active="{{ active }}" bindchange="onChange">
        <van-tab title="{{item.title}}" wx:for="{{select}}" wx:key="id">
            <my-orderc orderdatas="{{newData}}"></my-orderc>
        </van-tab>
    </van-tabs>
</view>